"""Crawler package for the public-source intelligence pipeline.

The package is organized as small pipeline stages rather than one large script.
That structure helps new developers follow a document from approved source,
through discovery and fetching, into extraction, cleaning, redaction, claim
extraction, trust scoring, storage, reporting, and API/UI presentation.
"""
